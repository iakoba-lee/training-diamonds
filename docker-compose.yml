services:
  training-diamonds:
    build: .
    container_name: training-diamonds
    restart: unless-stopped
    environment:
      - NODE_ENV=production
      - PORT=3001
    volumes:
      - ./data:/usr/src/app/data
    networks:
      - web

networks:
  web:
    external: true
